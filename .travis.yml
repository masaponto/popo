language: cpp
compiler:
    - clang
before_install:
    - sudo apt-get update
    - sudo apt-get install cmake
before_script:
    - pwd
script: "sh ./.travis_build.sh"
notifications:
    email: false
